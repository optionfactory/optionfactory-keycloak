# opfablank welcome theme: intentionally no parent, no styles, no scripts.
# Knobs understood by keycloak's WelcomeResource:
#   redirectToAdmin=true  -> redirect / to the admin console once an admin user exists
#                            (default false here: the blank page does not advertise the admin url)
